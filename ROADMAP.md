# 개발일정

[1주차](https://www.notion.so/1-2e7e9fb9ac18804aa77ae233311907d3?pvs=21)

프로젝트 시작을 축하드립니다! 개발 경력이 있으시니 SwiftUI의 선언형 문법에도 금방 적응하실 거예요. 오늘(1월 12일)부터 제출 시작일(2월 6일)까지 약 4주간의 집중 로드맵을 정리해 드립니다.

---

### 🛠️ 시작 전 환경 설정 (Day 0)

- **도구:** Mac의 **Xcode**를 사용하세요.
- **프로젝트 생성:** `File` > `New` > `Project`에서 반드시 **App Playground**를 선택해야 합니다. (제출 규격인 `.swiftpm` 파일이 생성됩니다.)
- **타겟:** iOS/iPadOS를 기본으로 설정하세요.

---

### 📅 1주차: UI 기초 및 메모리 그리드 구현 (1/12 ~ 1/18)

**목표:** 앱의 시각적 뼈대인 '메모리 칸'과 '데이터 구조'를 만듭니다.

1. **데이터 모델 설계:** * `MemorySlot` 구조체 생성 (id, 주소값, 실제 저장된 값, 타입 정보 포함).
2. **메모리 그리드 UI:** * `LazyVGrid`를 사용하여 4x4 또는 4x6 형태의 메모리 판을 구현합니다.
    - 각 칸(`MemoryCellView`)은 주소와 값이 보이도록 디자인합니다.
3. **상태 관리:** * `@State`를 사용하여 배열 형태의 메모리 데이터를 관리하고, 칸을 클릭했을 때 강조(Highlight)되는 기능을 넣습니다.

---

### 📅 2주차: 포인터 핵심 로직 및 인터랙션 (1/19 ~ 1/25)

**목표:** "가리킨다"는 개념을 코드로 구현하고 사용자의 조작에 반응하게 합니다.

1. **포인터 변수 구현:** * 특정 칸을 '포인터 변수'로 지정합니다. 이 칸은 다른 칸의 **주소값**을 값으로 가집니다.
2. **참조 및 역참조 로직:**
    - **참조(`&`):** 특정 칸을 선택하고 포인터에 연결하면 해당 주소가 저장되는 로직.
    - **역참조():** 포인터가 가진 주소를 따라가서 그곳의 실제 값을 가져오는 버튼/제스처 구현.
3. **코드 스니펫 연동:**
    - 사용자가 클릭할 때마다 하단에 `int *p = &a;` 같은 코드가 실시간으로 업데이트되도록 텍스트 뷰를 배치합니다.

---

### 📅 3주차: 애니메이션 및 레벨(게임) 디자인 (1/26 ~ 2/1)

**목표:** 시각적 완성도를 높이고 교육용 '게임'으로서의 재미를 추가합니다.

1. **화살표 애니메이션:** * 가장 중요한 단계입니다! SwiftUI의 `Path`와 `Shape`를 사용하여 포인터 변수에서 대상 변수로 뻗어 나가는 **역동적인 화살표**를 만듭니다.
    - `.animation(.spring())`을 활용해 값이 바뀔 때 화살표가 부드럽게 이동하게 합니다.
2. **시나리오(레벨) 구성:**
    - **Level 1:** 변수의 주소 찾기.
    - **Level 2:** 포인터를 이용해 값 수정하기.
    - **Level 3:** 이중 포인터(Pointer to Pointer) 도전하기.
3. **피드백 시스템:**
    - 잘못된 주소를 가리키면 화면이 흔들리거나 "Segmentation Fault" 경고가 뜨는 위트 있는 효과를 추가합니다.

---

### 📅 4주차: 최종 폴리싱 및 에세이 작성 (2/2 ~ 2/8)

**목표:** 제출물을 완성하고 버그를 잡습니다. (2/6 접수 시작!)

1. **App Playground 테스트:** * Mac뿐만 아니라 iPad의 **Swift Playgrounds 앱**에서도 잘 돌아가는지 최종 확인합니다. (심사위원들이 iPad로 확인할 확률이 높습니다.)
2. **에세이 작성 (중요!):** * **주제(400자 이내):** "왜 C 언어 포인터를 주제로 했는가?", "이 앱을 통해 어떤 문제를 해결하고 싶었는가?"를 작성합니다.
    - *팁: 본인이 처음 C 언어를 배울 때 겪은 어려움과, 이를 시각화로 극복하려 했다는 스토리를 추천합니다.*
3. **제출 파일 준비:**
    - `.swiftpm` 파일 용량을 확인하고(너무 큰 에셋 지양), 스크린샷 1~3장을 준비합니다.

---

### 💡 오늘 바로 해볼 일

Xcode를 켜고 **"App Playground"** 프로젝트를 만든 뒤, **`LazyVGrid`를 사용해 16개의 사각형을 화면에 띄워보세요.** 이 사각형들이 사용자님의 프로젝트의 시작점이 될 것입니다.

개발하시다가 SwiftUI 문법(예: `@State`와 `@Binding`의 차이)이나 화살표를 그리는 구체적인 코드가 필요하시면 언제든 물어봐 주세요! 도와드리겠습니다.