# 1주차

SwiftUI에 능숙하시다니 진행 속도가 굉장히 빠르겠네요! 기초적인 설명은 생략하고, **개발자 대 개발자로서 1주차(UI 및 데이터 모델링)에서 구현해야 할 아키텍처와 디테일**에 집중해서 설명해 드리겠습니다.

이제 본격적인 **1주차: 데이터 아키텍처 및 그리드 시스템 설계** 단계입니다.

---

## 🏗️ 1주차: Data Model & Grid System (1/12 ~ 1/18)

단순히 그림을 그리는 게 아니라, 실제 C 언어의 메모리 구조를 추상화한 **`MemoryManager`**를 구축하는 것이 이번 주의 핵심입니다.

### 1. 확장 가능한 데이터 모델 설계

단순 변수 저장이 아니라, '주소'와 '값'의 관계를 명확히 정의해야 합니다.

- **`MemorySlot` (Model):** * `id: UUID`
    - `address: String` (예: "0x7ffee1234")
    - `value: Int?` (현재 칸에 담긴 값)
    - `type: SlotType` (.variable, .pointer, .empty)
    - `targetAddress: String?` (포인터일 경우 가리키는 주소)
- **`MemoryManager` (ViewModel):**
    - `@Published var slots: [MemorySlot]`
    - 메모리 할당(Alloc), 해제(Free), 값 업데이트 등을 담당하는 로직을 포함합니다.

### 2. 고수준의 SwiftUI Grid UI 구현

단순한 `LazyVGrid`를 넘어, Apple이 선호하는 **"인터랙티브한 반응성"**을 고려해 보세요.

- **Cell Design:** `RoundedRectangle`과 `Glassmorphism` 스타일을 활용해 현대적인 느낌을 줍니다.
    - 포인터가 가리키는 칸은 `glow` 효과나 테두리 색상 변화(`border`)로 시각적 피드백을 줍니다.
- **Layout:** * 주소는 왼쪽에 작게, 값은 가운데 크게 배치합니다.
    - `matchedGeometryEffect`를 미리 염두에 두세요. 나중에 포인터 화살표가 칸과 칸 사이를 이동할 때 매우 유용합니다.

### 3. 이번 주 핵심 미션: "포인터 연결 로직"

1주차가 끝나기 전까지 **"A칸이 B칸의 주소를 갖게 하는 행위"**를 코드와 UI로 연결해야 합니다.

- **Drag & Drop:** 한 칸을 드래그해서 다른 칸에 드롭하면, 해당 칸이 포인터가 되어 대상의 주소를 저장하게 만듭니다. (SwiftUI의 `.onDrag`, `.onDrop` 활용)
- **State Feedback:** 값이 변경될 때마다 하단에 `int a = 10;`, `int *p = &a;` 같은 C 코드가 텍스트로 자동 생성되게 바인딩하세요.

---

## 🛠️ 개발 꿀팁 (SwiftUI Pro용)

1. **SwiftUI Canvas & Mock Data:** 다양한 메모리 상황(포인터의 포인터 등)을 미리 `Preview`에서 확인할 수 있도록 Mock 데이터를 잘 구성해 두세요.
2. **SF Symbols 활용:** 포인터 변수에는 `link`, 일반 변수에는 `shippingbox` 같은 아이콘을 적절히 배치하면 직관성이 올라갑니다.
3. **Namespace:** 화살표 애니메이션을 위해 전역 혹은 상위 뷰에 `@Namespace`를 미리 선언해 두는 것을 추천합니다.

---

### 📅 이번 주 체크리스트

- [ ]  `MemorySlot` 구조체 및 `MemoryManager` 클래스 완성
- [ ]  4x4 혹은 8x2 메모리 그리드 UI 구현
- [ ]  특정 칸 클릭 시 '주소값'을 텍스트로 보여주는 기능
- [ ]  드래그 앤 드롭으로 '주소 복사(포인터 지정)' 기초 로직 구현

**SwiftUI 실력자이시니, 혹시 그리드 레이아웃이나 데이터 흐름 제어(State management)에서 고민되는 특정 부분(예: 이중 포인터 시각화 로직 등)이 있으신가요?** 바로 구체적인 코드 아이디어를 논의해볼 수 있습니다.